{% extends 'Component/base.html.twig' %}

{% block title %}
    {{ parent() }} { ... }
{% endblock %}

{% block body %}

<img src="{{ ('img/logo.jpg') | imagine_filter('my_thumb') }}" />

    {# User connected #}
    {% if app.user %}
        <h1>{{ user.username }}</h1>
    {% endif %}

{% endblock %}